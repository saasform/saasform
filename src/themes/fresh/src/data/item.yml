name: "{{ item.name }}"
title: "{{ item.title | formatDot }}"
text: "{{ item.text | formatNoOrphanDot }}"
icon: "{{ item.icon }}"
image_url: "{{ item.image_url }}"
primary: "{{ item.primary }}"
button: "{{ item.button }}"
button_href: "{{ item.button_href | default: '/signup' }}"
price_text: "{{ item.price_text }}"
price_year: "{{ item.price_year }}"
price_month: "{{ item.price_month }}"
price_decimals: "{{ item.price_decimals }}"
