<!DOCTYPE html><html><head><title>{{ title }}</title>  <link rel="stylesheet" href="{{ root_assets }}/assets/style.css"></head><body>

<h1>Subscribe to plan</h1>

<form action="/user/billing/subscribe" method="POST">
  <div>
    <h2>Choose plan</h2>

    <ul>
      {% for plan in plans %}
        <li>
          <input type="radio" id="huey" name="plan" value="{{plan.id}}" />
          <label for="{{plan.id}}">{{plan.name}} {{plan.price_year}} ({{plan.price_month}} when billen monthly)</label>
        </li>
      {% endfor %}
    </ul>
  </div>

  <div>
    <h2>Choose payment method</h2>

    <ul>
      {% for method in account.payments_methods %}
      <li>
        <input type="radio" id="huey" name="method" value="{{method.id}}" />
        <label for="{{method.id}}">xxxx-xxxx-xxxx-{{ method.card.last4 }} expiring on {{ method.card.exp_month }}/{{ method.card.exp_year }}</label>
      </li>
      {% endfor %}
    </ul>

    <input type="checkbox" id="monthly" name="monthly" />
    <label for="monthly">Pay monthly</label>
  </div>

  <input type="hidden" name="_csrf" value="{{ csrfToken }}">

  <input type="submit" />
</form>



<div>
  <h2>Add a payment method</h2>

  <form action="/user/billing/payment" method="POST">

    <!-- Name -->
    <input name="paymentName" id="modalPaymentName" type="text" value="First Last">

    <!-- Card info -->
    <input name="number" id="modalPaymentNumbber" type="number" value="4242 4242 4242 4242">
    <input  name="exp_month" id="modalPaymentDate" type="text" value="11">
    <input  name="exp_year" id="modalPaymentDate" type="text" value="2023">
    <input  name="cvc" id="modalPaymentDate" type="text" value="123">

    <input type="hidden" name="_csrf" value="{{ csrfToken }}">

    <input type="submit" />
  </form>
</div>

</body>
</html>